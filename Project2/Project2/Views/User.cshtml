@model System.Data.DataTable

@{
    ViewData["Title"] = "Users";
}

<h1>@ViewData["Title"]</h1>


<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Email
            </th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Rows.Count; i++)
        {
            <tr>
                <td>
                    @Model.Rows[i]["Name"]
                </td>
                <td>
                    @Model.Rows[i]["Email"]
                </td>
                <td>
                    <a asp-action="Select" asp-route-id="@Model.Rows[i]["Id"]">Select</a>
                    <input id="item.UserId" type="button" value="Select" onclick="select(this)"/>
                </td>
            </tr>
        }
    </tbody>
</table>

<script>
    function select(item) {
        var itemId = item.id,
        var url = '@Url.Action("Select/", "User")';
        var data = { Id: itemId };
        $.post(url, data, function (result) {
            var id = '#postedFor' + postId;
            $(id).html(result);
        });
    }
</script>