@*@model System.Data.DataTable*@

@{
    ViewData["Title"] = "Users";
}

<h1>@ViewData["Title"]</h1>

@*<form asp-action="ShareWith">
</form>*@
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Email
            </th>
        </tr>
    </thead>
    @*<tbody>
        @for (int i = 0; i < Model.Rows.Count; i++)
        {
            <tr>
                <td>
                    @Model.Rows[i]["Name"]
                </td>
                <td>
                    @Model.Rows[i]["Email"]
                </td>
                <td>
                    <a asp-action="AddOrEdit" asp-route-id="@Model.Rows[i]["PhotoId"]">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@Model.Rows[i]["PhotoId"]">Delete</a> |
                    <a asp-action="ShareWith" asp-route-id="@Model.Rows[i]["PhotoId"]">Share with</a>
                </td>
            </tr>
        }
    </tbody>*@
</table>